services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - grocery-data:/data
    environment:
      - SECRET_KEY=${SECRET_KEY:-change-me-in-production}
      - DATABASE=/data/grocery.db
    restart: unless-stopped

volumes:
  grocery-data:
